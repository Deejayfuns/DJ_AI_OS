[project]
name = "dj-ai-os"
version = "0.1.0"
description = "DJ AI OS - desktop-first music library assistant for professional DJs"
requires-python = ">=3.10"

# Core deps required at import time by test-imported modules
# (AI modules gracefully handle missing heavy deps via try/except)
dependencies = [
    "customtkinter",
    "mutagen",
    "cryptography",
    "pygame",
    "librosa",
    "numpy",
    "soundfile",
    "audioread",
    "fastapi",
    "uvicorn",
    "pydantic",
    "SpeechRecognition",
    "PyAudio",
    "pyttsx3",
    "edge-tts",
    "sounddevice",
    "vosk",
    "windnd",
    "opencv-python",
    "requests",
    "beautifulsoup4",
    "mido",
    "torch",
    "torchaudio",
    "pyyaml",
    "msgpack",
    "watchdog",
    "zeroconf",
    "sqlalchemy",
    "asyncpg",
    "aiosqlite",
    "alembic",
    "stripe",
    "yt-dlp",
    "scipy",
    "httpx",
]

[project.optional-dependencies]
server = [
    "httptools",
    "watchfiles",
    "websockets",
    "python-dotenv",
]
dev = [
    "pytest",
]
neural = []

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
